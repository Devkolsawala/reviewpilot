name = "reviewpilot-cron"
main = "src/index.ts"
compatibility_date = "2024-09-23"

[triggers]
# Runs every hour — adjust to taste (CF free tier: up to 3 cron triggers)
# Cron syntax: minute hour day-of-month month day-of-week
crons = ["0 * * * *"]
