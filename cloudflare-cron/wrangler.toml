name = "reviewpilot-cron"
main = "src/index.ts"
compatibility_date = "2024-09-23"

[triggers]
# All plans sync every 2 hours (previously every hour with plan-gated windows).
# Cron syntax: minute hour day-of-month month day-of-week
crons = ["0 */2 * * *"]
